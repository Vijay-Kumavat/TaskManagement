import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '5px', backgroundColor: '#007bff', color: 'white' }}>
            <h1>Task Tracker</h1>
        </header>
    );
};

export default Header;
